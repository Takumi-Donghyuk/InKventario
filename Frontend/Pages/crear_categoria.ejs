<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crear categoría</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-purple-50 to-indigo-100 flex flex-col items-center min-h-screen p-6">
  <!-- Botón de regreso -->
  <div class="w-full max-w-2xl mb-6 text-center">
    <a href="/admin" 
       class="inline-block bg-emerald-500 text-white px-5 py-2 rounded-lg 
       shadow hover:bg-emerald-600 transition">
       ⬅ Volver al panel principal
    </a>
  </div>

  <!-- Tarjeta principal -->
  <div class="w-full max-w-2xl bg-white shadow-xl rounded-2xl p-10">
    <!-- Encabezado -->
    <div class="text-center mb-8">
      <h2 class="text-3xl font-bold text-gray-800">Crear nueva categoría</h2>
      <p class="text-gray-500">Defina y organice los productos dentro de su sistema</p>
    </div>

    <!-- Formulario -->
    <form action="/categorias/crear" method="POST" class="space-y-6">
      <div>
        <label for="nombre" class="block text-gray-700 font-medium mb-2">Nombre de la categoría *</label>
        <input type="text" name="nombre" required
               class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-purple-500" 
               placeholder="Ejemplo: Electrónica, Ropa, Alimentos">
      </div>

      <div>
        <label for="descripcion" class="block text-gray-700 font-medium mb-2">Descripción (opcional)</label>
        <textarea name="descripcion" rows="4"
                  class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-purple-500"
                  placeholder="Agregue una breve descripción de la categoría..."></textarea>
      </div>

      <!-- Botón -->
      <div class="text-center">
        <button type="submit"
                class="w-full bg-emerald-500 text-white font-semibold py-3 
                rounded-lg hover:bg-emerald-600 transition shadow-md">
          Guardar categoría
        </button>
      </div>
    </form>
  </div>

</body>
</html>
