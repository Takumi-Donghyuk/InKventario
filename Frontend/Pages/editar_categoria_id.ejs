<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modificar categoría con id</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col items-center p-6">

    <!-- Botón de regreso -->
    <div class="w-full max-w-md mb-6">
        <a href="/categorias/editar" 
           class="block text-center bg-emerald-500 text-white font-semibold py-2 px-4 rounded hover:bg-emerald-600 transition">
           ⬅ Volver a la lista de categorías
        </a>
    </div>

    <div class="w-full max-w-2xl bg-white shadow-md rounded-lg p-8">
        <h1 class="text-2xl font-bold mb-6 text-center text-gray-800">Editar categoría</h1>

        <form action="/categorias/editar/<%= categoria.id_categoria %>" method="POST" class="space-y-4">
            
            <div>
                <label for="nombre" class="block text-gray-700 mb-1 font-semibold">Nombre *</label>
                <input type="text" name="nombre" value="<%= categoria.nombre %>" required
                       class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-emerald-400" />
            </div>

            <div>
                <label for="descripcion" class="block text-gray-700 mb-1 font-semibold">Descripción</label>
                <textarea name="descripcion" rows="4"
                          class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-emerald-400"><%= categoria.descripcion %></textarea>
            </div>

            <button type="submit"
                    class="w-full bg-emerald-500 hover:bg-emerald-600 text-white py-2 px-4 rounded font-semibold transition">
                Actualizar categoría
            </button>
        </form>
    </div>

</body>
</html>
