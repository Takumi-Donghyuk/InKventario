<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Iniciar Sesión</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="../Styles/loginstyle.css">
</head>
<body class="flex h-screen items-center justify-center bg-gray-100">

  <form action="/login" method="POST" class="bg-white p-6 rounded-2xl shadow-lg w-80">
    <h1 class="text-3xl font-extrabold text-blue-800 mb-6">Papelería El Bunker</h1>
    <h2 class="text-2xl font-bold mb-4 text-center">Iniciar Sesión</h2>

    <% if (typeof error !== "undefined") { %>
      <p class="text-red-500 text-center mb-2"><%= error %></p>
    <% } %>

    <input
      type="email"
      name="correo"
      placeholder="Correo"
      class="w-full p-2 mb-3 border rounded-lg"
      required
    />

    <input
      type="password"
      name="contrasena"
      placeholder="Contraseña"
      class="w-full p-2 mb-3 border rounded-lg"
      required
    />

    <button
      type="submit"
      class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600">
      Ingresar
    </button>
  </form>

</body>
</html>


