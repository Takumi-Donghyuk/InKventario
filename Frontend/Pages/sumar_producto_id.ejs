<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>Sumar stock</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-b from-red-50 to-red-200 min-h-screen p-6">

  <!-- Botón de regreso -->
  <div class="w-full max-w-2xl mb-6 flex justify-center">
    <a href="/productos/sumar" 
       class="bg-red-500 text-white px-6 py-2 rounded-full 
       shadow-lg hover:bg-red-600 transition-all transform hover:scale-105">
       ⬅ Volver a lista de productos
    </a>
  </div>

  <main class="max-w-xl mx-auto">
    <h1 class="text-3xl font-extrabold text-red-800 mb-8 text-center">
      Sumar stock a <%= producto.nombre %>
    </h1>

    <div class="bg-white rounded-3xl shadow-xl p-8">
      <div class="flex flex-col items-center mb-6">
        <% if (producto.url_imagen) { %>
          <img src="<%= producto.url_imagen %>" alt="Imagen de <%= producto.nombre %>" 
               class="w-24 h-24 object-cover rounded-xl shadow-md mb-4">
        <% } %>
        <p class="text-red-700 text-lg">
          Cantidad actual: <strong><%= producto.cantidad %></strong>
        </p>
      </div>

      <form action="/productos/sumar/<%= producto.id_producto %>" method="POST" class="space-y-4">
        <label for="cantidad" class="block text-red-800 font-semibold">Cantidad a añadir</label>
        <input type="number" id="cantidad" name="cantidad_a_sumar" min="1" required
               class="w-full px-4 py-2 rounded-lg border border-red-300 focus:ring-2 focus:ring-red-500">
        
        <button type="submit"
                class="w-full bg-red-500 text-white px-6 py-3 rounded-lg shadow 
                       hover:bg-red-600 transition-all font-semibold">
          ➕ Sumar stock
        </button>
      </form>
    </div>
  </main>
</body>
</html>
